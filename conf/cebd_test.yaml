##### hydra
hydra:
    run:
      dir: ./outputs/${exp_name}/${status}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    sweep:
      dir: ./outputs/${exp_name}/${status}/${now:%Y-%m-%d}_${now:%H-%M-%S}
      subdir: ${hydra.job.override_dirname}
# hydra:
#     run:
#       dir: /ssd/0/zzh/tmp/CEBD/outputs/${exp_name}/${status}/${now:%Y-%m-%d}_${now:%H-%M-%S}
#     sweep:
#       dir: /ssd/0/zzh/tmp/CEBD/outputs/${exp_name}/${status}/${now:%Y-%m-%d}_${now:%H-%M-%S}
#       subdir: ${hydra.job.override_dirname}


##### dir
exp_name: code_dev/BDNeRV_RC  # experiment name
tester_name: cebd_tester # tester name: cebd_realtester | cebd_tester
status: test       # run status: train|test|real_test
checkpoint: ${hydra:runtime.cwd}/outputs/code_dev/BDNeRV_RC/record/bak_ssim-loss/2023-05-09_16-21-47/checkpoints/model_latest.pth  # loading checkpoint ${hydra:runtime.cwd}
outputs_dir: outputs/

##### run
gpus: [1]           # GPU used, Warning: Only one GPU is supported for 'test' now
num_workers: 2      # number of cpu worker
save_img: false      # save the reconstructed images

# params
sigma_range: 0
test_sigma_range: 0
frame_n: 8
trainer:
  epochs: 300

##### metrics
metrics:
  - _target_: srcs.metric.metric_iqa.IQA_Metric
    metric_name: psnr
  - _target_: srcs.metric.metric_iqa.IQA_Metric
    metric_name: ssim
  - _target_: srcs.metric.metric_iqa.IQA_Metric
    metric_name: lpips

##### data&nets
defaults:
  - data: vid_test_data
  - network: cebd  # for fswd_traj_tester
  - hparams: ced_hyparams # for fswd_traj_tester

  - override hydra/job_logging : custom # custom || colorlog
  - override hydra/hydra_logging: colorlog
  - _self_